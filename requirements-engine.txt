# 엔진 venv(.venv) — 음성 합성 전용. 콘솔(.venv-app)과 분리한다.
#
# ★ 왜 나누나. onnxruntime 이 콘솔 의존성과 충돌하고, 렌더가 도는 중에도
#   콘솔을 닫을 수 있어야 한다. 두 쪽은 **파일로만** 대화한다
#   (scripts/tts_bridge.py 의 job.json / result.json).
#
# ★ 여기 적힌 것이 곧 `tts/` 가 실제로 import 하는 전부다. 슈퍼토닉3 원본에는
#   CLI·서버(typer·rich·fastapi)도 딸려 있었지만 우리는 엔진만 쓰므로
#   그 모듈들을 벤더링에서 걷어냈다 — 안 쓰는 의존성을 새 PC 마다 깔 이유가 없다.

onnxruntime>=1.17     # 또는 onnxruntime-gpu — 정확히 하나만 설치
numpy>=1.26
soundfile>=0.12.1
pyyaml>=6.0

# 모션 리마스터(fmt 10 경로)에서만 쓴다. fmt 11 은 HyperFrames 가 굽는다.
Pillow>=10
